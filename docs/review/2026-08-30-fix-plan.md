# 再レビュー（2026-08-30）修正計画

## 対応状況（2026-08-31）

| 指摘 | 状態 | PR |
|---|---|---|
| High 1 退会状態の正本 | ✅ 完了 | #21 |
| High 2 トランザクション化 | ✅ 完了（A1/A2/B/C/D） | #23 #24 #25 #26 #27 |
| High 3 マイグレーション CI 検証 | ✅ 完了 | #22 |
| Medium 1 カレンダー冪等性 | ✅ 完了 | #34 |
| Medium 2 通知 Cron ページング | ✅ 完了 | #30 |
| Medium 3 JST 月境界 | ✅ 完了 | #28 |
| Medium 4 空き状況の部分成功 | ✅ 完了 | #32 |
| Medium 5 同意チェック fail closed | ✅ 完了 | #31 |
| Medium 6 匿名回答の文言 | ✅ 完了（文書修正のみ） | #29 |
| Medium 7 連携カレンダーの実メール | ✅ 完了 | #33 |
| Low 1 実DB・RLS・E2E テスト | 🟡 一部（`db-tests` CI ジョブで RPC は実 postgres 検証。ロール別 RLS 全網羅とブラウザ E2E は未着手） | — |
| Low 2 未使用 eventCategory | ✅ 完了 | #35 |

新規マイグレーション: 040（退会印バックフィル）、041〜045（RPC 群）、046（カレンダー sync_state）。
すべて CI の `migrations` ジョブで適用検証済み。

## 前提

- 対象 HEAD: `c29ff469d19b72821e22cc233095ed2c13b0e20a`（origin/main と一致、ワークツリー clean）
- 2026-08-30 の再レビュー結果を、最新コードで対象行を確認したうえで実施順に並べたもの。
- レビューに書かれていない問題は追加していない。
- 2026-08-30 の grill-me で下記の決定を確定済み（末尾「確定した決定」参照）。

## 実行方針（grill-me 確定）

- **スコープ**: High 3項目 + Medium 7項目。Low 2項目（実DBテスト整備・lint 警告）は別途。
- **実装体制**: 全タスクを Codex（codex-rescue）へ委譲。Claude Code は設計doc作成・diff レビュー・
  テスト実行・検証を担当。CLAUDE.md の `codex-delegation` に従い、各タスクで提案→承認→ハンドオフ→独立検証。
  TDD 必須（RED を実際に確認してから実装）。
- **進め方**: この計画を確定してコミット → High から1項目ずつ「設計doc（`docs/superpowers/specs/`）→
  Codex 実装 → Claude Code 検証」で回す。設計docと実装計画はフローどおり都度コミットする。
- **スキーマ変更の承認**: 新規マイグレーション（RPC・カラム追加・RLS）は、各タスクの設計docに
  具体の SQL を書いてから個別承認を取る。CLAUDE.md によりスキーマは無断で変更しない。
- **PR**: 項目ごとに小さい PR。High 2 は RPC ごとに分ける。

## 検証で分かった行番号のズレ

レビュー記載の行番号は数行ずれている箇所がある。実装時は下記の実行番号を使う。

| レビュー記載 | 実際 | 対象 |
|---|---|---|
| lib/actions/account/account.ts:54 | 55 | 物理削除の `Promise.all` |
| lib/actions/account/account.ts:86 | 82-91 | `updateUserById` で `user_metadata.withdrawn_at` を書く |
| lib/actions/settlement/settlements.ts:366 | 368 | 費用 insert |
| middleware.ts:160 | 137 のみ | 退会判定は 137 の1箇所（160 は同意チェックで無関係） |
| app/api/cron/notifications/route.ts:21 | select 21 / `.limit(200)` 26 | |

その他（plans.ts:74/147、settlements.ts:226/449/567、calendar.ts:30/74/90、
calendar-events.ts:60-72、availability/route.ts:82/106、callback/route.ts:37/50、
middleware.ts:167/176/192）はレビュー記載どおり。

---

## High 1. 退会状態を `user_metadata` で判定している

**現状**
- `lib/actions/account/account.ts:82-91` が `admin.auth.admin.updateUserById` で
  `user_metadata: { ...withdrawn_at }` を書く。
- `middleware.ts:137` が `isWithdrawnUserMetadata(user.user_metadata)` で退会ゲートを判定。
- 同じファイル `middleware.ts:163-164` には「`user_metadata` は本人が `auth.updateUser()` で
  書き換えられるので印に使えない」というコメントがあり、同意チェックは `app_metadata` を見ている。
  退会ゲートだけ矛盾している。
- `account.ts` は `profiles.deleted_at` も立てている（migration 023 で列は存在）。middleware は未参照。
- `/api/*` は `middleware.ts` の `isPublicRequest` で素通り。退会ゲートが効くのは画面遷移だけ。

**修正内容（確定）**
- 既存の同意印と対称な構造にする。
  - 正本 = `profiles.deleted_at`
  - キャッシュ = `app_metadata.withdrawn_at`（service role のみ書ける。本人は偽装不可）
  - 参照ヘルパーは `lib/domain/account/legal-consent.ts` / 書き込みは `lib/auth/legal-consent-mark.ts` に倣う
- `account.ts` の退会処理で `app_metadata.withdrawn_at` を書く。`user_metadata.withdrawn_at` は廃止。
- `middleware.ts` の退会ゲートを「`app_metadata` に印があれば退会。無ければ `profiles.deleted_at` を引く」に変更。
- Server Action の共通ガードと、退会済みが触れる RPC で退会チェックを追加。対象範囲は設計docで洗い出す。
- RLS で `profiles.deleted_at IS NOT NULL` を弾くかは設計docで判断（入れるならマイグレーション、個別承認）。
- 既存ユーザー向けに `user_metadata → app_metadata` バックフィルのマイグレーション（026 に倣う）。

**触るもの**
- `lib/actions/account/account.ts`、`middleware.ts`、`lib/auth/*`（判定・書き込みヘルパー）、
  退会チェックを入れる各 Server Action、`supabase/migrations/040_*.sql`（バックフィル、RLS）。

**テスト**
- 退会後に `user_metadata` / `app_metadata` を改ざんしたセッションで、middleware・Server Action・
  （入れるなら）RLS が拒否すること。
- 既存の退会テストが `user_metadata` 前提なら、RED を確認してから新実装に合わせて直す（スキップ・削除はしない）。

---

## High 2. 複数テーブル更新が非トランザクションで、一部エラーも無視

**現状（すべて確認済み）**

| 箇所 | 問題 |
|---|---|
| `plans.ts:74` 作成 | plan insert 後、`participants`/`candidate_dates`/`share_links`/`plan_reminder_settings` を `Promise.all` で挿入（:112-115）。1つ失敗で plan 行と一部の子だけ残る。cleanup なし。 |
| `plans.ts:147` 編集 | `availability_answers` 削除（:163）→ `candidate_dates` 削除（:172）→ 新 `candidate_dates` insert（:189）。insert 失敗時、旧候補日と回答が消えた状態でエラー返却。 |
| `settlements.ts:226` 再計算 | `settlements` の unpaid を delete（:253）→ 再 insert（:256）→ `plans.settlement_status` update（:271）。途中失敗で精算が消える。`settlement_payments` は精算行に `ON DELETE CASCADE`。 |
| `settlements.ts:449` 費用編集 | `expenses` update（:449）→ `expense_splits` 全 delete（:465）→ 再 insert（:466）→ `recomputeSettlements`（:478）。 |
| `settlements.ts:368` 費用作成 | `expenses` insert → `expense_splits` insert（:384）→ 再計算。 |
| `settlements.ts:567` 支払い登録 | `settlement_payments` insert（:567）→ `settlements` status update（:592）→ `plans` update（:600）→ 通知・監査（:602-603）。後半のエラーを見ていない。同時実行で精算行が競合。 |
| `account.ts:55` 退会 | 11 個の `delete`、`profiles`/`event_members` の `update`、storage 削除、`updateUserById` の結果を一切確認していない。 |

**修正内容（確定）**
- 非トランザクションな一連の更新をすべて PL/pgSQL 関数（RPC）に移し、関数内の暗黙トランザクションで実行する。
  既存の `034_connection_calendar_rpc.sql` / `mark_plan_settling` と同じパターン。
- 対象は「予定作成 / 日程編集 / 精算再計算 / 費用作成・編集 / 支払い登録 / 退会の DB 部分」。
  **RPC の分割粒度（1本にまとめるか分けるか）は各設計docで決める。**
- 同時実行対策: RPC 冒頭で対象 plan / settlement を `SELECT ... FOR UPDATE` でロックし直列化。
- RPC の権限は 032 に合わせて `revoke ... from anon`。
- 新規マイグレーション（`040_*.sql` 以降）。各設計docで SQL を提示して個別承認。
- 退会の DB 部分は RPC 化。ただし外部（Auth API・storage）を含むので全体は単一トランザクションにできない
  → **`profiles.deletion_state`（`pending` / `done`）を追加**し、手順ごとに結果を確認して失敗をログ、
  途中失敗でも再実行で回復できるようにする。

**触るもの**
- `supabase/migrations/040_*.sql` 以降（新規、個別承認）。
- `lib/actions/plan/plans.ts`、`lib/actions/settlement/settlements.ts`、`lib/actions/account/account.ts`。

**テスト**
- RPC を実 PostgreSQL で叩く統合テスト。制約違反を注入して途中失敗時にロールバックされること。
- 2 セッション同時支払いで `settlement_payments` 合計がガード（migration 021）を超えないこと。
- 退会を途中で失敗させ、再実行で `done` に到達すること。
- 既存の Server Action テストは RPC 化に合わせて期待値を更新。

---

## High 3. セットアップ文書が最新マイグレーションに追従していない

**現状（確認済み）**
- `README.md:85-103` は 001–019 しか列挙していない。
- `docs/release-checklist.md:24-54` は 001–031 まで。
- 実ファイルは `039_connection_action_rate_limit.sql` まで存在。
- `supabase/` 配下は `migrations/` のみ。`config.toml` なし、Supabase CLI 連携なし、
  `.github/workflows/ci.yml` にマイグレーション適用ステップなし。適用は SQL Editor 手作業だけ。
- **本番 Supabase には 032〜039 は適用済み（ユーザー確認済み）。** 本番の不整合リスクは現時点ではない。
  再発防止（新環境・今後のズレ）のための対応。

**修正内容（確定）**
- Supabase CLI は導入しない。`.github/workflows/ci.yml` に「一時 Postgres（`services: postgres`）へ
  `migrations/*.sql` を番号順に `psql` 適用 → 失敗で赤」のステップを追加する。
- `README.md` と `docs/release-checklist.md` の手動列挙をやめ、「`supabase/migrations/` を
  番号順にすべて適用」と書き換える。
- CI で全マイグレーションがクリーン適用されることを継続的に検証。

**触るもの**
- `.github/workflows/ci.yml`、`README.md`、`docs/release-checklist.md`、CI 用の適用スクリプト（新規）。
- `config.toml` は追加しない（設定ファイル変更なし）。

**規模**: 中。

---

## Medium 1. Google カレンダーイベント作成が冪等でない

**現状**
- `calendar.ts:48` で `google_calendar_event_id` の有無を確認 → `calendar.ts:74` で Google に insert
  → `calendar.ts:90` で DB に id を書く。90 の update はエラーを見ていない。
- 二重クリック・複数タブ・Google 成功後の DB 失敗で重複作成。招待メールも重複。

**修正内容（確定）**
- `plans` に `google_calendar_sync_state`（`idle` / `creating` / `created`）を追加。
- `idle → creating` の条件付き update（`WHERE sync_state = 'idle'`）に成功した実行だけ Google API を叩く。
- Google 側イベントの `iCalUID` に `plan-{planId}` 相当の安定 ID を設定。
  DB update 失敗時は次回アクセスで `creating` を検出し、`iCalUID` で既存イベントを照合してから id を補完。
- 新規マイグレーション（`sync_state` 列）。設計docで SQL を提示して個別承認。

**触るもの**
- `lib/actions/calendar/calendar.ts`、`lib/google-calendar/*`、`supabase/migrations/*`（列追加、個別承認）。

**テスト**: 二重呼び出しで Google insert が1回だけ。DB 失敗注入時に重複しないこと。

---

## Medium 2. 通知 Cron が新しい 200 件だけを処理

**現状**
- `app/api/cron/notifications/route.ts:21-26` が `plans` を `created_at desc` + `.limit(200)` で取得。ページングなし。
- 対象ステータス（`collecting_answers` / `date_confirmed`）が 200 件超で古い予定が毎回対象外。

**修正内容（確定）**
- カーソル方式で全件処理する（A案）。`created_at` 昇順 + `id` タイブレークでページ分割し、
  対象がなくなるまで回す。件数上限を実質撤廃。
- DB 側で対象を絞る根本対応（B案）は今回やらない。必要なら別タスク。

**触るもの**: `app/api/cron/notifications/route.ts`。

**テスト**: 200 件超のデータで全件に通知が作られること。
`tests/notification/cron-notifications-route.test.ts` を拡張。

**規模**: 小。

---

## Medium 3. Google カレンダー月表示の範囲が UTC 基準

**現状**
- `lib/google-calendar/calendar-events.ts:60-72` の `monthTimeRange` が
  `Date.UTC(year, monthIndex, 1)` / `Date.UTC(year, monthIndex+1, 1)` を `timeMin` / `timeMax` に渡す。
- このアプリは JST 固定（`lib/shared/format.ts:6` / `const JST = "Asia/Tokyo"`）。
  9 時間ずれて月初 0:00–9:00(JST) が抜け、翌月初 0:00–9:00(JST) が混入。
- `tests/calendar/calendar-events.test.ts:59` が現在のずれた値を期待値にしている。
- 呼び出しは `app/api/google-calendar/freebusy/route.ts:53` の1経路のみ。

**修正内容（確定）**
- 月境界を `Asia/Tokyo` で組み立てて ISO 文字列にする。
  `timeMin = {year}-{month}-01T00:00:00+09:00`、`timeMax` は翌月同刻。
- アプリは JST 固定なので固定オフセット `+09:00` でよい（DST なし）。コメントで JST 固定を明記。
- `tests/calendar/calendar-events.test.ts` の期待値を正しい境界に修正（現状追認のテストを直す）。

**触るもの**: `lib/google-calendar/calendar-events.ts`、`tests/calendar/calendar-events.test.ts`。

**規模**: 小。

---

## Medium 4. 参加者1人の Google API 障害で全員の空き時間取得が失敗

**現状**
- `app/api/events/[eventId]/availability/route.ts:82` が `Promise.all` で全連携者の freeBusy を取得。
- 1人が 401/403 で throw → `:106-108` の catch が全体を 409 `calendar_reconnect_required` で返す。
- `components/plan/group-availability-calendar.tsx:131` 付近の再接続案内が、問題の参加者ではなく
  閲覧中ユーザーに出る。

**修正内容（確定）**
- `Promise.allSettled` にして部分成功を許可。取得できた人数で集計を返す。
- レスポンスに「取得失敗人数」を含める。UI は「N人ぶんの予定を取得できませんでした」と件数だけ表示。
- 再接続ボタンは、**閲覧者自身の連携が切れている場合にだけ**出す。他人の再接続は促さない
  （失敗した参加者名は出さない）。

**触るもの**: `app/api/events/[eventId]/availability/route.ts`、
`components/plan/group-availability-calendar.tsx`、`buildDailyBusySummaries` 周辺。

**テスト**: 3人中1人が 401 でも残り2人の集計が返ること。UI の件数表示。

**規模**: 中。

---

## Medium 5. 同意・初期設定の DB 確認がエラー時に通過する

**現状**
- `middleware.ts:176-186` 同意チェック: `if (!consentError && !consent)` のときだけリダイレクト。
  `consentError` が真なら素通り。
- `middleware.ts:192-202` プロフィールチェックも `if (!profileError)` で同じ挙動。

**修正内容（確定）**
- **規約同意は fail closed**。`consentError` のときは安全なエラーページへ誘導し、
  エラーを `console.error`（または既存の監査ログ）に残す。
- **プロフィール（オンボーディング）は fail open のまま**。ただし fail open である旨をコメントで明記し、
  `profileError` 時に警告ログを残す。

**触るもの**: `middleware.ts`、エラーページ（無ければ新規）。

**テスト**: `user_consents` クエリがエラーを返すモックで、エラーページに飛ぶこと。
`profiles` クエリのエラーではページ表示が続き、警告ログが出ること。

**規模**: 小〜中。

---

## Medium 6. 匿名回答について文書・画面と実装が矛盾

**現状**
- `README.md:22`「共有リンクによる未ログイン回答」、`docs/design/01_requirements.md:45`「未ログイン回答」、
  `app/plans/[planId]/page.tsx:411`「未ログインの人もそのまま回答できます。」
- 実装は逆。`middleware.ts:69-83` が `/s/` を非公開にし、`app/s/[token]/answer/page.tsx:39-45` も
  未ログインなら `/login` へリダイレクト。

**修正内容（確定）**
- 現在の「ログイン + 参加者登録必須」を正しい仕様とする。実装は変更しない。
- 文言を実装に合わせて直す:
  - `README.md:22` — 「共有リンクによる回答（要ログイン。先に参加者として招待）」相当に
  - `docs/design/01_requirements.md:45` — 同上
  - `app/plans/[planId]/page.tsx:411` — 「回答にはログインが必要です。先に参加者として招待してください」相当に

**触るもの**: `README.md`、`docs/design/01_requirements.md`、`app/plans/[planId]/page.tsx`。

**規模**: 小。

---

## Medium 7. 連携カレンダーのメールアドレスにログイン用アドレスを保存

**現状**
- `app/api/google-calendar/callback/route.ts:50` が `account_email: user.email`
  （= アプリログイン用 Supabase ユーザーのメール）を保存。
- Calendar OAuth で別の Google アカウントを選んでも、保存されるのはログイン用アドレス。
- `lib/actions/calendar/calendar.ts:67-71` がこの `account_email` を招待先メールに使う。
- `components/calendar/calendar-connection-card.tsx:50` 付近で誤ったアドレスが表示される。

**修正内容（確定）**
- Calendar OAuth のスコープに `userinfo.email` を追加し、トークン交換のレスポンス（`id_token` または
  userinfo エンドポイント）から Calendar 側の実メールアドレスを取得して `account_email` に保存する。
- OAuth スコープ追加は同意画面に影響するため、`docs/phase2-google-calendar-setup.md` の更新と
  ユーザーへの周知を含める。

**触るもの**: `app/api/google-calendar/callback/route.ts`、`lib/google-calendar/*`（トークン交換）、
`docs/phase2-google-calendar-setup.md`。

**テスト**: 別アカウントを選んだ callback で、保存される `account_email` が Calendar 側のアドレスになること。

**規模**: 中。

---

## スコープ外（別途）

### Low 1. 実 DB・RLS・同時実行を検証するテストが不足
- 単独タスクにはしない。High 2 / High 3 の各タスクに「実 PostgreSQL を使う統合テストを含める」形で吸収する。
  一時 DB は Medium/High 3 で CI に入れる `services: postgres` を流用。
- ブラウザ E2E（作成→回答→確定→精算）は今回のスコープ外。

### Low 2. 未使用の `eventCategory` 引数
- `components/plan/plan-form.tsx:464` の props 未使用、lint 警告 1 件（確認済み）。
- 今回のスコープ外。対応するなら、利用予定を確認したうえで呼び出し元含めて削除し、単独コミットにする。

---

## 実施順序

1. **High 3** — CI に psql マイグレーション適用ステップ追加 + 文書書き換え。他タスクの実 DB テストの土台。
2. **High 1** — 退会状態を `profiles.deleted_at` + `app_metadata` へ移行。設計doc で RLS の要否を確定。
3. **High 2** — 非トランザクション更新の RPC 化。RPC 単位でサブタスク化し、それぞれ実 DB テスト付き。
   退会の DB 部分と `deletion_state` もここ。
4. **Medium 1 / 3 / 7** — Google カレンダー: 冪等性（sync_state + iCalUID）/ JST 境界 / OAuth 実アドレス取得。
5. **Medium 2 / 5** — Cron カーソル処理 / 同意チェックの fail closed 化。
6. **Medium 4** — 空き時間取得の部分成功対応。
7. **Medium 6** — 文書・画面文言の修正。

各項目: 設計doc（`docs/superpowers/specs/`）→ 個別承認（スキーマ変更がある場合）→ Codex 実装 →
Claude Code が diff レビュー + テスト実行 + 検証 → 項目ごとに PR。

---

## 確定した決定（2026-08-30 grill-me）

| # | 決定 |
|---|---|
| スコープ | High 3 + Medium 7。Low 2 は別途。 |
| 実装体制 | 全タスク Codex 委譲。設計doc・レビュー・検証は Claude Code。TDD 必須。 |
| High 1 正本 | `profiles.deleted_at` 正本 + `app_metadata.withdrawn_at` キャッシュ（同意印と対称）。`user_metadata.withdrawn_at` 廃止。 |
| High 3 本番状況 | 032〜039 は本番適用済み（確認済み）。再発防止のみ対応。 |
| High 3 仕組み | Supabase CLI は入れない。CI に psql 適用スクリプトを追加。文書は「番号順に全部」へ。 |
| スキーマ承認 | 新規マイグレーションは各設計doc に SQL を書いてから個別承認。 |
| High 2 方式 | 非トランザクション更新を全て RPC 化（`FOR UPDATE` ロック）。分割粒度は設計doc。 |
| 退会処理の頑健性 | `profiles.deletion_state`（pending/done）を追加し再実行可能に。 |
| Medium 1 | `plans.google_calendar_sync_state` + Google 側 `iCalUID` に安定 ID。 |
| Medium 2 | カーソルで全件処理（A案）。B案は今回やらない。 |
| Medium 3 | 月境界を `Asia/Tokyo`（固定 +09:00）で組む。テスト期待値を修正。 |
| Medium 4 | `Promise.allSettled` で部分成功。失敗人数だけ表示。再接続案内は閲覧者本人の連携が切れている時のみ。 |
| Medium 5 | 同意のみ fail closed（エラーページ + ログ）。プロフィールは fail open + 警告ログ。 |
| Medium 6 | 認証必須が正。README / requirements.md / plan ページの文言を実装に合わせて修正。 |
| Medium 7 | OAuth スコープに `userinfo.email` を追加し Calendar 側の実アドレスを保存。 |
| PR | 項目ごとに小さい PR。High 2 は RPC ごと。 |
| 次のステップ | この計画を確定コミット → High 1 の設計doc から着手。 |
